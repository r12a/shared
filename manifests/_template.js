
langTag='th' 					// used by functions(initialiseSummary)
scriptTag='thai'				// used by functions; prompts
notesLangtag='th'				// used by show_codepoints

explainerDir='thai/'			// used by block-functions; functions (doHeadersFooters)
blockDir='thai/th'				// block; show_codepoints; block-functions; egcode; functions
pickerDir='thai/'				// show_codepoints; block-functions; prompts; functions
scriptSummaryTableName='th'		// functions(initialiseSummary)
charUsageBCP='th'				// prompts
orthogName = 'Thai'				// prompts
indic = true					

pageWebfonts = ["Noto Serif Thai WF"]	// functions(makeFontChanger)
defaultSize = '24px'			// block, manageFonts, block-functions, functions



// block settings

languageName = 'Thai'			// block, show_codepoints, block-functions, prompts
letterClassName = 'th'			// block
lang = 'th'						
direction = ''					// used in showNameDetails to set direction
